# Production runtime dependencies for the Creative Validation web app.
#
# The app itself only needs Flask; HTML parsing uses the Python stdlib
# (html.parser), so beautifulsoup4/lxml are intentionally NOT required.
# OCR (pytesseract/Pillow) is an optional soft-import and is left out of
# the default deploy to keep the image small — add it only if you need
# image-based creatives.
Flask==3.1.3
gunicorn==23.0.0
