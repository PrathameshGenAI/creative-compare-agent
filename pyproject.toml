[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "creative-compare-agent"
version = "0.1.0"
description = "Local deterministic creative comparison scorecard agent"
requires-python = ">=3.10"
authors = [{name = "Local MVP"}]
dependencies = []

[project.optional-dependencies]
web = ["flask"]

[project.scripts]
creative-compare = "creative_compare_agent.cli:main"
